<%= link_to 'Back', posts_path %> | <%= link_to 'Edit', edit_post_path(@post) %>
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
<p>
	<strong>By:</strong> 
	<%= @user.first_name %> 
	<%= @user.last_name %> 
</p>
<p>
  <strong>Text:</strong>
  <%= @post.text %>
</p>

<%= link_to 'Back', posts_path %> | <%= link_to 'Edit', edit_post_path(@post) %>

<h2>Comments</h2>
<%= render @post.comments %>

<h2>Add a commemt:</h2>
<%= render "comments/form" %>

